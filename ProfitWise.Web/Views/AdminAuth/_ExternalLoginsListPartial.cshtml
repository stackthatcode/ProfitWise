@model ProfitWise.Web.Models.ExternalLoginListViewModel
@using Microsoft.Owin.Security

<h4>Login for Shop Users</h4>
<hr />
@{
    var loginProviders = Context.GetOwinContext().Authentication.GetExternalAuthenticationTypes();

    using (Html.BeginForm("Login", "ShopifyAuth")) {
        @Html.AntiForgeryToken()
        <div id="socialLoginList">
            <div class="form-group">
                <label>Enter Full Shop Url <span style="font-weight: 400;">e.g. shopname.myshopify.com</span></label>
                <input class="form-control" name="shop" id="shop" style="margin-bottom: 8px;" />
            </div>
            @foreach (AuthenticationDescription p in loginProviders)
            {
                <button type="submit" class="btn btn-default" id="@p.AuthenticationType" name="provider" 
                        value="@p.AuthenticationType" title="Log in using your @p.Caption account">@p.AuthenticationType</button>
            }
        </div>
    }
}
